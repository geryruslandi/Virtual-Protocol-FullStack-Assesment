export enum QUEUE_EVENTS {
  GENERATE_MATCH_CANDIDATE = 'generate_match_candidate',
}

export enum QUEUE_NAMES {
  CANDIDATE_GENERATOR_QUEUE = 'candidiate_generator_queue',
}

export enum QUEUE_SERVICES {
  SCHEDULER = 'scheduler',
  WORKER = 'worker',
}
